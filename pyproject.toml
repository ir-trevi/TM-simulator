[project]
name = "TM-simulator"
version = "1.1.2"
authors = [{name = "M Trevi"}]
description = "A Turing Machine simulator written in python with a terminal interface"
readme = "README.md"
license = {text = "MIT"}

[project.scripts]
tm-simulator = "TMsimulator:main"

[project.urls]
Homepage = "https://github.com/ir-trevi/TM-simulator/"
